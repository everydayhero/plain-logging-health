#!/bin/bash -ei

bucket="${RELEASE_ENVIRONMENT}_S3_IMAGE_BUCKET"

docker-compose run -e S3_IMAGE_BUCKET=${!bucket} app npm run gulp
[[ ${BUILDKITE[@]} ]] && buildkite-agent artifact upload "artifacts/${RELEASE_ENVIRONMENT}.zip"
